<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <artifactId>deegree-ogcapi-webapp</artifactId>
  <packaging>pom</packaging>
  <name>deegree-ogcapi-webapp</name>
  <description>Webapps for OGC API Implementations</description>

  <parent>
    <groupId>org.deegree</groupId>
    <artifactId>deegree-ogcapi</artifactId>
    <version>1.3.4</version>
  </parent>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-war-plugin</artifactId>
          <!-- Add Swagger UI resources to the war file. -->
          <configuration>
            <failOnMissingWebXml>false</failOnMissingWebXml>
            <webResources combine.children="append">
              <resource>
                <directory>${project.build.directory}/swagger-ui/META-INF/resources/webjars/swagger-ui/${swagger-ui-version}</directory>
                <includes>
                  <include>**/*.*</include>
                </includes>
                <targetPath>swagger-ui</targetPath>
              </resource>
              <resource>
                <directory>${project.build.directory}/deegree-ogcapi-documentation</directory>
                <includes>
                  <include>**/*.*</include>
                </includes>
                <targetPath>deegree-ogcapi-documentation</targetPath>
              </resource>
            </webResources>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.eclipse.jetty</groupId>
          <artifactId>jetty-maven-plugin</artifactId>
          <version>11.0.24</version>
          <configuration>
            <webApp>
              <contextPath>/deegree-ogcapi</contextPath>
              <allowDuplicateFragmentNames>true</allowDuplicateFragmentNames>
              <containerIncludeJarPattern>^$</containerIncludeJarPattern>
              <!--
              <webInfIncludeJarPattern>.*/.*deegree-ogcapi-[^/]\.jar$</webInfIncludeJarPattern>
              -->
            </webApp>
            <webAppSourceDirectory>target/${project.artifactId}-${project.version}</webAppSourceDirectory>
            <scanIntervalSeconds>0</scanIntervalSeconds>
            <stopPort>8079</stopPort>
            <stopKey>stopit</stopKey>
            <httpConnector>
              <port>8080</port>
              <idleTimeout>60000</idleTimeout>
            </httpConnector>
            <systemProperties>
              <org.eclipse.jetty.util.log.class>org.eclipse.jetty.util.log.Slf4jLog</org.eclipse.jetty.util.log.class>
              <log4j2.configurationFile>log4j2.properties</log4j2.configurationFile>
            </systemProperties>
          </configuration>
          <executions>
            <execution>
              <id>start-jetty</id>
              <goals>
                <goal>stop</goal>
                <goal>start</goal>
              </goals>
            </execution>
            <execution>
              <id>stop-jetty</id>
              <goals>
                <goal>stop</goal>
              </goals>
            </execution>
          </executions>
          <dependencies>
            <dependency>
              <groupId>org.slf4j</groupId>
              <artifactId>slf4j-api</artifactId>
              <version>${slf4j.version}</version>
            </dependency>
            <dependency>
              <groupId>org.apache.logging.log4j</groupId>
              <artifactId>log4j-core</artifactId>
              <version>${log4j.version}</version>
            </dependency>
            <dependency>
              <groupId>org.apache.logging.log4j</groupId>
              <artifactId>log4j-slf4j-impl</artifactId>
              <version>${log4j.version}</version>
            </dependency>
          </dependencies>
        </plugin>
        <plugin>
          <!-- Download Swagger UI webjar. -->
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <executions>
            <execution>
              <id>swagger-ui</id>
              <phase>prepare-package</phase>
              <goals>
                <goal>unpack</goal>
              </goals>
              <configuration>
                <artifactItems>
                  <artifactItem>
                    <groupId>org.webjars</groupId>
                    <artifactId>swagger-ui</artifactId>
                    <version>${swagger-ui-version}</version>
                    <outputDirectory>${project.build.directory}/swagger-ui</outputDirectory>
                  </artifactItem>
                  <artifactItem>
                    <groupId>${project.groupId}</groupId>
                    <artifactId>deegree-ogcapi-documentation</artifactId>
                    <classifier>deegree-ogcapi-html</classifier>
                    <type>zip</type>
                    <version>${project.version}</version>
                    <outputDirectory>${project.build.directory}</outputDirectory>
                  </artifactItem>
                </artifactItems>
                <excludes>
                </excludes>
              </configuration>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <!-- Replace the OpenAPI specification example URL with the local one. -->
          <groupId>com.google.code.maven-replacer-plugin</groupId>
          <artifactId>replacer</artifactId>
          <executions>
            <execution>
              <phase>prepare-package</phase>
              <goals>
                <goal>replace</goal>
              </goals>
            </execution>
          </executions>
          <configuration>
            <file>
              ${project.build.directory}/swagger-ui/META-INF/resources/webjars/swagger-ui/${swagger-ui-version}/index.html
            </file>
            <replacements>
              <replacement>
                <token>url:</token>
                <value>requestInterceptor: function (req) {if (req.href == window.location.href) { req.headers = {'Accept': 'application/vnd.oai.openapi+json;version=3.0'};}; return req;}, url:</value>
              </replacement>
              <replacement>
                <token>"https://petstore.swagger.io/v2/swagger.json"</token>
                <value>window.location.href</value>
              </replacement>
              <replacement>
                <token>href="./</token>
                <value>href="./api/</value>
              </replacement>
              <replacement>
                <token>src="./</token>
                <value>src="./api/</value>
              </replacement>
            </replacements>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>deegree-ogcapi-features</artifactId>
      <version>${project.version}</version>
      <scope>runtime</scope>
    </dependency>
    <!-- deegree-core -->
    <dependency>
      <groupId>org.deegree</groupId>
      <artifactId>deegree-featurestore-sql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.deegree</groupId>
      <artifactId>deegree-featurestore-simplesql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.deegree</groupId>
      <artifactId>deegree-featurestore-memory</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.deegree</groupId>
      <artifactId>deegree-featurestore-shape</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.deegree</groupId>
      <artifactId>deegree-connectionprovider-datasource</artifactId>
      <scope>runtime</scope>
    </dependency>
  </dependencies>

  <modules>
    <module>deegree-ogcapi-webapp-oracle</module>
    <module>deegree-ogcapi-webapp-postgres</module>
  </modules>

</project>